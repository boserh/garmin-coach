# Garmin access (two providers, switch via GARMIN_PROVIDER)
garth>=0.4.47            # old, deprecated — works while the token is valid
garminconnect>=0.2.0     # new, native auth (gconn provider)

# Claude API
anthropic>=0.40.0

# Telegram bot (with job-queue for scheduling)
python-telegram-bot[job-queue]>=21.0

# .env
python-dotenv>=1.0.0

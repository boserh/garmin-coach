# Беклог garmin_bot

Напрям розвитку: **від "розумного звіту" до адаптивного AI-тренера** — замкнути петлю
дані → аналіз → план → виконання → порівняння план/факт → автоадаптація.

Аудит 2026-07 (беклог vs код vs ринок, вердикти, RICE нових фіч):
[ANALYSIS.md](ANALYSIS.md).

**Пріоритет (2026-07, після NF-04/05/06):** колонка «Пріор.» у таблицях нижче —
🔴 highest / 🟠 high / 🟡 medium / 🟢 low / ⚪ lowest. Усі highest/high закрито:
EP-12 (RPE-тренд/болі в адаптацію+дайджест+звіт), EP-08 (проактивні health-алерти),
CODE-01 (розбити analysis-сервіс) і ST-09 (реальні вправи силової в звіті) —
✅ зроблено (див. Done). З 🟡 medium далі закрито **EP-09** (`/ask` над усією
історією), **NF-03** (режим хвороба/подорож), **NF-08** (особистий MCP-сервер) і
**EP-04** (веб-дашборд). Наступний — EP-05. Frozen-тікети — ⚪.

## Сторі покращення (S/M)

Немає відкритих — усі закрито (див. Done).

## Епіки (L/XL)

| ID | Пріор. | Назва | Оцінка | Залежності |
| --- | --- | --- | --- | --- |
| [EP-05](EP-05-race-pack.md) | 🟡 medium | Race pack — підготовка до перегонів | L | фаза 0: типізувати `target_date`; GAP-модуль спільний з EP-15 |
| [EP-11](EP-11-web-coach-chat.md) | 🟢 low | Веб-чат з тренером | L | EP-09 бажано |
| [EP-10](EP-10-multisport.md) | 🟢 low | Мультиспорт: вело/плавання у планах і аналізі (фаза навантаження вже в NF-05) | XL | фаза 2 (навантаження) → NF-05 |
| [EP-15](EP-15-elevation-gap.md) | 🟢 low | Рельєф і grade-adjusted pace (GAP) | M–L | GAP-модуль реюзабельний (його чекає EP-05 фаза 2) |
| [EP-03](EP-03-strength-progression.md) | 🟢 low | Прогресія силових | L | коли силові стануть регулярними |
| [EP-16](EP-16-season-periodization.md) | ⚪ lowest | Сезонна періодизація (кілька стартів) — треба 2-й старт | XL | EP-02 ✅, EP-05; intake спільний з NF-05 |
| [EP-06](EP-06-saas-quotas.md) | ⚪ lowest | SaaS-режим: квоти вартості · ❄️ **frozen**: чужі юзери на неофіційному API, який Garmin закриває — після OPS-01 + 3 міс. стабільності | XL | рішення про продукт |

## Нові фічі (аудит 2026-07; деталі й RICE-розбір — ANALYSIS.md §3)

Немає відкритих — NF-03/NF-08 закрито (див. Done).

## Перфоманс

| ID | Пріор. | Назва | Оцінка | Залежності |
| --- | --- | --- | --- | --- |
| [PERF-01](PERF-01-parallel-user-jobs.md) | ⚪ lowest | Паралелізація per-user джоб бота · ❄️ **frozen**: за 1–2 юзерів болю немає, тригер >5 юзерів | M | CODE-04 перед; PERF-03 для >2 |
| [PERF-03](PERF-03-postgres-and-indexes.md) | ⚪ lowest | Postgres перед мультиюзером + індекси · ❄️ **frozen** (Postgres-частина): прив'язаний до `/register` — той самий garth-ризик, що EP-06. **Аудит індексів зроблено** окремо (композитні `user_id`+дата) — див. Done | M | — |

## Безпека й операції (аудит коду 2026-07)

Обидві сторі цього розділу зроблено (2026-07) — див. Done (SEC-01, OPS-02).
Операційну частину OPS-02 на Pi також завершено: systemd-таймер увімкнено,
off-SD `--rsync-dest` веде на USB (`/mnt/backup/garmin/`), restore-тест на
реальних даних пройдено (2026-07).

## Оптимізації коду (рефакторинги)

Немає відкритих — CODE-02/03/07 закрито (див. Done).

## Рекомендований порядок (2026-07, за ANALYSIS.md §4.1)

**Негайно (виживання):**

1. **OPS-01** ✅ — розвідка python-garminconnect + задокументований план міграції +
   моніторинг падіння логіну зроблені (2026-07); rate limit/backoff зроблено в
   **PERF-05** ✅ (`client._api` — той самий шар клієнта, куди сяде й міграція).
2. **PERF-02** ✅ — Claude-дедуп у таблиці `llm_cache` (спільній для бота й веба),
   Garmin-кеш у per-key файлах; старий `garmin_cache.json` seed'иться один раз
   (2026-07).
3. **PERF-04a** ✅ — bcrypt → `asyncio.to_thread` через async-обгортки в
   `app.core.crypto`; async-роути (login/register/зміна пароля/створення юзера)
   більше не морозять event loop (2026-07).
4. **OPS-02** ✅ — бекапи `garmin.db`: скрипт проти втрати річної історії
   (SD-картка — найтиповіша відмова Pi); systemd-таймер + off-SD rsync на USB
   увімкнено на Pi (2026-07). ST-08 і SEC-01 — короткі, за нагодою
   (SEC-01 стає блокером лише перед відкриттям `/register`).

**Quick wins (1–2 тижні кожен, високий ефект):** CODE-04 ✅ → EP-07 ✅ (недільний
дайджест; недільний пайплайн ще закласти під злиття з EP-02-пропозиціями і
EP-13-погодою — поки окрема джоба о 19:00) →
**EP-12** ✅ MVP (RPE/болі — годують усе наступне; лишились фази-споживачі) →
**EP-13** ✅ (погодна корекція плану — щоденна легка джоба; липнева спека) →
**CODE-05** ✅ (спільний report-флоу бот/веб/morning) → **ST-03** ✅ (погода в
on-demand `/report`) → **EP-14** ✅ (особисті рекорди) — філери.

**Стратегічні ставки (місяць+ кожна, це і є моат):** **NF-01** ✅ (підсилює звіти,
EP-08-пороги і NF-06) → **EP-09** ✅ (движок для EP-11 і NF-08) → **NF-05** ✅
(коректність адаптації для реального мультиспорт-профілю) → **EP-04** ✅ (продуктове
відчуття без LLM-витрат).

**Експерименти (дешеві, перевіряють гіпотези):** NF-02 (один ручний прогін
кореляцій — чи є знахідки взагалі), **NF-08** ✅ (вихідні + подвійне використання
EP-09-інструментів), NF-07 (раз і в задоволення).

**Заморожено до стабілізації auth:** EP-06 (SaaS), PERF-03 (Postgres), PERF-01
(паралелізація), відкриття `/register`.

Решта — за станом: EP-03 — коли силові стануть регулярними; EP-15 — щойно
з'являться горбисті маршрути; EP-16 — коли буде другий старт у календарі;
EP-10 (аналіз вело) і ST-05 — за запитом/філери.

## Done

| ID | Назва | Де реалізовано |
| --- | --- | --- |
| [EP-04](EP-04-web-dashboard.md) | Веб-дашборд — продуктове відчуття без LLM-витрат | `app/charts.py` — SVG-хелпери (`series`/`trend_series`/`run_series`/`run_charts`) винесено з `app/routers/admin.py` (і дубльованого `me.py::_trend_series`) в один спільний модуль, обидва роутери реюзають без зміни поведінки. `app/routers/dashboard.py::GET /dashboard` — чистий DB-рід (нуль Garmin/Claude): «сьогодні» через реюз `me._latest_ring` (той самий hero-ring, що на `/me`), 30-денні тренди HRV/RHR/сон/стрес (`_trend_charts`, hover), найближчі 7 днів активного плану (`repository.list_workouts(upcoming_only=True)`, зрізано вікном), останні 5 активностей (реюз `me._act_meta`/`_pace_str`), вартість AI поточного місяця (новий `repository.month_cost` — SUM `cost_usd` з початку календарного місяця UTC, реюзне пізніше EP-06). Порожні стани (нема історії/плану/активностей) — окремі підказки з лінками на `/settings`/`/plan`. Логін не-адміна (`routers/auth.py::login_submit`) і кореневий `/` (`app/main.py`) тепер ведуть на `/dashboard` замість `/settings`/`/me`; адмінський логін — без змін (`/ui`). PWA-мінімум: `app/static/manifest.json` + `app/static/icon.svg`, лінковані лише зі сторінки дашборда. `tests/test_dashboard.py` + `test_repository.py::test_month_cost_sums_current_month_only` |
| [NF-08](NF-08-personal-mcp-server.md) | Особистий read-only MCP-сервер над БД (експеримент) | `app/mcp_server.py` — тонкий stdio MCP-шар (`mcp.server.fastmcp.FastMCP`, опційна залежність `pip install -e ".[mcp]"`) над тими самими read-only user-scoped інструментами, що EP-09 `/ask` (`query_activities`/`query_daily`/`aggregate_weekly`/`get_activity_detail`/`get_training_plan` — усі йдуть через єдину точку диспетчеризації `app.analysis.reports._run_ask_tool`, без дублювання валідації/капів). Однокористувацький процес: `--email` резолвиться в `user_id` один раз при старті (`_resolve_user_id`), кожен виклик інструменту відкриває свіжу сесію. Нуль Garmin-викликів, нуль LLM-витрат з нашого боку (платить підписка клієнта MCP). Запуск: `./venv/bin/python -m app.mcp_server --email me@example.com`, підключення — `claude_desktop_config.json` або `claude mcp add`. `tests/test_mcp_server.py` |
| [NF-03](NF-03-sickness-travel-mode.md) | Режим «хвороба/подорож» — ремонт плану одним тапом | `SYSTEM_SICK` (`app/analysis/prompts.py`) + `run_sick_check`/`sick_with_stats` (`app/analysis/plans.py`, той самий `_plan_ops_with_stats`-рушій, що edit/adapt/weather) — консервативна перебудова блоку: skip пропущених/найближчих днів, полегшення (modify) кількох сесій після повернення, ре-рамп ≤10%/тиждень; формулювання не медичні. `_filter_sick_ops` обмежує дії до move/modify/skip у вікні `today-14..today+14` (`SICK_LOOKBACK_DAYS`/`SICK_WINDOW_DAYS`) — той самий guard-патерн, що в EP-13-погоді. Команда `/sick [днів]` (`bot/handlers.py::sick`) — опційний аргумент «скільки днів пропущено», ✅/❌ через наявний `plan_callback`/`pending_plan`-флоу (жодного нового callback-хендлера). `ReportLog(kind="sick")`. Не dedup-кешується (як і edit/adapt/weather). `tests/test_sick.py` |
| [NF-07](NF-07-quarterly-wrapped.md) | Річний/квартальний огляд («Wrapped») | `app/wrapped.py` — чистий (нуль LLM) добір періоду (`period_window` year=52тиж/quarter=13тиж, `parse_period`, `has_signal`, `label`/`fmt_range`, `build_context`); `repository.wrapped_stats` **реюзає `window_stats`** + додає крос-спорт розклад (`multisport.sport_bucket`), загальний час, найоб'ємніший тиждень і дугу VO2max (перше vs останнє); `records_in_range` — рекорди періоду. `service.run_wrapped` наративізує **один естетичний Opus-лонгрід** (`SYSTEM_WRAPPED`, `MODEL_WRAPPED`), dedup-кеш `_wrapped_cache_key`, `ReportLog(kind="wrapped")`, `None` на порожньому вікні. Команда `/wrapped [рік\|квартал]` (чистий DB-рід + 1 Opus-виклик, без Garmin/MFA). `tests/test_wrapped.py` |
| [NF-02](NF-02-correlation-engine.md) | Кореляційний движок — «що на тебе насправді діє» | `app/correlations.py` — чистий (нуль LLM) щомісячний прохід: `find_correlations` тестує фіксований набір лагових пар (сон→HRV+1д, стрес→HRV, RHR→HRV…) з гейтами проти false-positives — `MIN_SAMPLES`=30, `\|r\|`≥`R_THRESHOLD`=0.35 **і** Fisher-z 95%-CI без нуля (`_fisher_ci_excludes_zero`, numpy-free); `pearson`/`_paired` (лаг-свідомий, індексує по ISO-даті, дірка бекфілу просто дропає пару). Нічого значущого → чесний `None` (нуль Claude). `service.run_insights` наративізує через **один Sonnet-виклик** (`SYSTEM_INSIGHTS`, кореляція≠причинність), dedup-кеш `_insights_cache_key`, `ReportLog(kind="insights")`. Команда `/insights` + місячний автоблок у дайджесті (`_monthly_insights_for_user`, guard `insights:<YYYY-MM>`). `tests/test_correlations.py` |
| [CODE-03](CODE-03-remove-legacy-paths.md) | Прибрати legacy `WEB_TOKEN` / `GARTH_TOKEN_DIR` | Видалено `app/core/security.py::verify_token` + `WEB_TOKEN` (Settings, `dependencies.py`, docs, conftest) — витіснений cookie-сесіями; `GARTH_TOKEN_DIR` прибрано з Settings, legacy глобальні garth/gconn-провайдери читають модульну константу `~/.garth`, `import-garth-token` отримав `--path` (дефолт `~/.garth`). `gconn` і `GARMIN_PROVIDER` не чіпано (OPS-01) |
| [CODE-02](CODE-02-cli-push-plan-reuse-plan-sync.md) | CLI `push-plan` поверх `plan_sync` | `plan_sync.select_forward(days, only_date)` (відбір вікна/pushable/skip-запушених) і `plan_sync.select_pushed(only_date)` — спільні для `sync_plan_to_garmin` і CLI `push-plan`/`unpush-plan` (правило «що пушабельне» в одному місці); CLI лишає свої `--dry-run` та ігнор `garmin_sync_enabled`. `tests/test_plan_sync.py` |
| [CODE-07](CODE-07-import-fit-series-refactor-tests.md) | Розплутати `import_fit_series` + тести | 96-рядкова функція з вкладеністю 8 розбита на чисті хелпери: `read_fit_activity` (стрім однієї FIT-стрічки за раз, early-bail, `(target, points)`), `build_targets` (start-ms→run), `_iter_fit_bytes` (плоский генератор над zip'ами), + `_ts_ms`/`_record_speed`/`_descend_to_di`/`_load_series_targets`. Оркестратор — одинарний цикл; поведінка (fills-nulls-only, точний матчинг за start, ідемпотентність, опційний `fitparse`) незмінна. Тести без справжніх FIT (fitparse-фейки): матчинг, близький-старт-не-крос, non-activity, speed-fallback, series-вже-є. `tests/test_export_import.py` |
| [EP-09](EP-09-ask-full-history.md) | `/ask` над усією історією — перший tool-use агент у проєкті | `run_ask_agent` (`app/analysis/reports.py`): до `MAX_ASK_ROUNDS`=5 раундів `client._complete_tools` (Sonnet, `SYSTEM_ASK_TOOLS`) над 5 read-only user-scoped інструментами (`_ask_tools`/`_run_ask_tool`) — `query_activities`/`query_daily` (нові `repository.query_activities`/`query_daily`, капнуті на `ASK_MAX_ROWS`=200, `query_daily` по білому списку `ASK_DAILY_FIELDS`), `aggregate_weekly` (run-volume або будь-яка `ASK_DAILY_FIELDS`-метрика по тижнях), `get_activity_detail` (реюзає `activity_payload` — сегменти, не сирий `series`), `get_training_plan` (`repository.query_training_plan` — активна `TrainingPlan` + її сесії за період; додано другим заходом, бо без нього питання про саму ПРОГРАМУ, а не факт, не мали за що зачепитись — жоден інший інструмент не читає `TrainingPlan`/`PlannedWorkout`). Питання без потреби в історії відповідається за 1 раунд без tool-викликів (recent_reports/recent_qa як і раніше). Хард-ліміт раундів + `MAX_ASK_TOTAL_TOKENS`=60k — перевищення повертає чесний `ASK_LIMIT_TEXT`, ніколи не кидає помилку. Дедуп-кеш на питання + `repository.latest_daily_date` (pure-DB, без Garmin). `ReportLog.tool_rounds` (нова колонка, міграція `f19ea8917ca6`) робить вартість багатораундового питання видимою. **Бот-only** — вебовий `GET /ask` свідомо не робили (продуктове рішення власника); бот-хендлер перейшов на `load_credentials` (як `/compare`, без ризику MFA). `tests/test_ask_agent.py` + нові тести в `tests/test_repository.py`/`test_analysis.py` |
| [ST-09](ST-09-strength-exercises-in-report-context.md) | Ранковий звіт бачить реальні вправи силової (не вигадує) | `reports._strength_exercises` (пріоритет `strength_plan` → `strength_snapshot`) підключено в `plan_today`; `SYSTEM` секція `plan_today` каже спиратись лише на `plan_today[].exercises`, не на `recent_activities`; `_UserGarthProvider.connectapi`/`username`/`display_name` тепер ліниво логіняться (defense-in-depth — будь-який Garmin-виклик поза `build_payload_cached` тепер автентифікований), полагоджено day-frame `previous_report` — усе PR #134. Другорядне з цього PR: гучний лог `PLAN strength snapshot empty tid=<id>` замість тихого ковтання порожнього `fetch_workout_full`/`fetch_workouts` (`app/analysis/plans.py::_add_plan_strength`) + ідемпотентний бекфіл наявних активних планів (CLI `backfill-strength-snapshots --email`, `app/cli.py`) — фільтрує «порожнє» в Python (JSON-null gotcha), кешує live-фетч по `tid`, ніколи не перезаписує наявний снапшот. Тести на `_strength_exercises` — `tests/test_analysis.py` |
| [EP-08](EP-08-health-alerts.md) | Проактивні health-алерти (аномалії відновлення) | `app/health.py` — чистий (нуль LLM) детектор recovery-аномалій: реюзає особисті персентиль-коридори NF-01 (`baselines.compute_baselines`) як пороги й ловить метрику, що кілька днів поза коридором у поганий бік — `hrv_low`/`rhr_up`/`sleep_debt`/`stress_high` → `Alert`/`HealthReport` (calibrating/none/alert), cold-start gate `HEALTH_MIN_HISTORY_DAYS`. `service.build_health_alerts`/`run_health_alert` (Sonnet `SYSTEM_HEALTH` + детермінований fallback `health.summary`, `ReportLog(kind="health")`), хук `_health_check_for_user` у morning-тіку (per-rule cooldown `alert:<kind>`, скіп якщо injury вже пінганув сьогодні — 1 risk-DM/день), команда `/health`, тумблери `HEALTH_ALERTS` + per-user `User.alerts_enabled` (форма `/settings`, міграція `b1c2d3e4f5a6`); `tests/test_health.py` |
| [CODE-01](CODE-01-split-analysis-service.md) | Розбити `analysis/service.py` на пакет | `app/analysis/` пакет: `client.py` (пул клієнтів, `PRICES`, `_complete`, `CallStats`, `AnalystError`), `cache.py` (дедуп-ключі + білдери контексту), `reports.py` (analyze/ask/activity/digest/compare/injury), `plans.py` (генерація/едити/адаптація/погода/силові); `service.py` — тонкий фасад із реекспортами (зовнішні імпорти незмінні), `prompts.py` не чіпано. Нуль поведінкових змін (тест-сьют зелений). PR #127 |
| [ST-01](ST-01-morning-report-plan-context.md) | Ранковий звіт бачить сьогоднішнє тренування з плану | `plan_today` наскрізь у `app/analysis/service.py` (`analyze_with_stats` + cache key) |
| [ST-02](ST-02-extra-metrics-in-reports.md) | `extra`-метрики (readiness, ACWR, RHR) у щоденних звітах | `fitness`-знімок у `run_analysis`/`analyze_with_stats` (`app/analysis/service.py`) |
| [ST-03](ST-03-weather-in-ondemand-report.md) | Погода в on-demand `/report` і `/report.json` | `weather.forecast_for_user(user)` (спільний async-хелпер, `app/weather.py`) — прокидається у `delivery.build_report` з `bot/handlers.py::report`, `app/routers/reports.py::report_json` і `bot/jobs.py::_deliver_morning` (замінив локальний `_fetch_user_weather`); `weather` уже в dedup-cache key; `tests/test_routers.py::test_report_json_forwards_weather` |
| [ST-04](ST-04-auto-activity-analysis.md) | Автоаналіз нової пробіжки після синку | `_activity_watch_for_user` у `bot/jobs.py` (вбудований у morning-тік) |
| [ST-06](ST-06-remote-mfa-relogin.md) | Remote MFA re-login | `app/garmin/mfa.py` + `/settings`-флоу (⚠️ спирається на garth-логін — подальша доля в OPS-01) |
| [EP-01](EP-01-plan-vs-actual-matching.md) | План/факт: матчинг виконаних тренувань | `app/garmin/matching.py` + `tests/test_matching.py` |
| [EP-02](EP-02-adaptive-plan.md) | Адаптивний план (замикання петлі) | `plan_adapt_job`/`_adapt_morning_check` у `bot/jobs.py`, `User.plan_adapt_enabled`, `tests/test_plan_adapt*.py` |
| [ST-07](ST-07-plan-adjust-level.md) | Adjust level — межі автоадаптації плану | `intake["adjust_level"]` + `plan_adjust_level`/`_filter_ops_to_level` (`app/analysis/service.py`), правила рівнів у `SYSTEM_PLAN_ADAPT`, вибір на setup-формі + зміна на `/plan` без перегенерації |
| [ST-08](ST-08-validate-exercise-names.md) | Валідувати назви вправ (не лише категорії) перед пушем | `exercises.check_exercise` (`app/garmin/exercises.py`, лог `EXERCISE invalid:`) у `_sanitize_strength` + swap-гілці `apply_plan_ops` (`app/garmin/repository.py`); `exercises_for` живить `exercise_variants` у `run_plan_edit`-контексті + `SYSTEM_PLAN_EDIT`; `tests/test_plan.py` |
| [OPS-01](OPS-01-garmin-auth-plan-b.md) | Garmin auth: «план Б» готовий у шухляді (сама міграція — за фактом поломки garth) | Маркери `GARMIN AUTH FAIL` (`app/garmin/mfa.py`, `providers.py`), `app.cli token-expiry` + `app/garmin/token_info.py`, `scripts/ops01_recon_gconn.py` (recon на Pi: 0 FAIL, garminconnect 0.3.6), план міграції в тікеті. Rate limit — далі в PERF-05 |
| [PERF-02](PERF-02-dedup-cache-to-db.md) | Дедуп-кеші з JSON-файлів у БД (крос-процесний баг) | Таблиця `llm_cache` + `app/db/llm_cache.py` (get/put у `run_analysis`/`run_ask`/`run_activity_analysis`; ключі `_cache_key` недоторкані); Garmin-кеш — per-key файли в `GARMIN_CACHE_DIR` з одноразовим seed'ом зі старого `garmin_cache.json` (`client._seed_legacy_cache`); `tests/test_llm_cache.py` + `tests/test_garmin_disk_cache.py` |
| [PERF-04a](PERF-04a-bcrypt-off-event-loop.md) | bcrypt поза event loop | `hash_password_async`/`verify_password_async` у `app/core/crypto.py` (`asyncio.to_thread`); async-роути `app/routers/auth.py` + `app/routers/settings.py` `await`-ять їх (sync-версії лишились для CLI) |
| [EP-12](EP-12-post-run-checkin.md) | Пост-тренувальний check-in (RPE + біль) — MVP + фази 2–3 | MVP: `ActivityRecord.subjective` (JSON `{rpe, pain?, note?}`) + `repository.set_subjective`/`get_last_activity`; бот `checkin_keyboard`/`checkin_callback` (RPE 1–10 + біль-кнопки, stateless по `ci:` callback data) чіпляється до автоаналізу (`bot/jobs.py`) і `/activity`; ручна `/checkin`; фаза 1 — `activity_payload` віддає `subjective` у `SYSTEM_ACTIVITY` (і в cache key); `/me` показує RPE/біль. **Фази 2–3**: чистий `app/subjective.py` (`summarize` → `{n, avg_rpe, rpe_rising, recurring_pain?, recent}`) живить (2) EP-02-адаптацію (`run_plan_adaptation` контекст + `SYSTEM_PLAN_ADAPT`, не кешується), (3a) дайджест — `overreached` у `repository.weekly_compliance` (легка сесія, виконана, але RPE ≥8; всередині `compliance`, вже в `_digest_cache_key`) + `SYSTEM_DIGEST`, (3b) ранковий звіт — `run_analysis`→`analyze_with_stats` `subjective` **і `_cache_key`** + секція «САМОПОЧУТТЯ» у `SYSTEM`; `tests/test_subjective.py`+`test_checkin.py` |
| [EP-13](EP-13-weather-aware-week.md) | Погодо-свідоме планування тижня | `weather.fetch_forecast_week` + `find_weather_conflicts` (чистий фільтр: ключова сесія на екстрем-день — спека/злива/вітер/ожеледь у межах `WEATHER_DECISION_DAYS`; без конфлікту — нуль Claude-викликів), `SYSTEM_WEATHER_PLAN` + `run_weather_plan_check`/`weather_plan_with_stats` (`app/analysis/service.py`, лише move/modify у вікні, ніколи skip/add; `ReportLog(kind="weather")`), `weather_plan_job`/`_weather_plan_for_user` (`bot/jobs.py`, щоденна `run_daily` о `WEATHER_PLAN_HOUR`, гейт: локація + активний план + `plan_adapt_enabled`), перевикористовує `_send_adapt_proposal`/`adapt_callback`; `_has_pending_proposal` — спільний гейт «одна пропозиція за раз» у всіх трьох хуках (пастка «не смикати двічі»); `tests/test_weather.py` + `tests/test_weather_plan_jobs.py` |
| [NF-01](NF-01-personal-baselines.md) | «Сьогодні vs твоя норма» — персональні базлайни | `app/baselines.py` — чистий (нуль LLM) детектор: rolling-персентилі (p25/p50/p75) за 90 днів по `resting_hr`/`hrv_avg`/`sleep_score`/`sleep_h`/`stress_avg`/`bb_charged` з `repository.read_history` → компактний `norm`-знімок ({cur, p50, band, n, pos}); `MIN_SAMPLES=14`-гейт. Прокидається у `run_analysis` (звіт/ранок, не `/deep`) → `analyze_with_stats` (`user_content["norm"]`) **і в `_cache_key`** (наскрізна пастка). Секція «ТВОЯ НОРМА» + валентність у `SYSTEM` (`app/analysis/prompts.py`). `tests/test_baselines.py` |
| [EP-14](EP-14-personal-records.md) | Особисті рекорди й віхи | `app/records.py` — чистий детектор (нуль LLM): бести по цілих пробіжках (найшвидші 5/10 км ±5%, найдовша дистанція/тривалість, найоб'ємніший ISO-тиждень) + all-time VO2max/прогнози забігів з `daily_metrics.extra` (race — поріг ≥10 с). Таблиця `PersonalRecord` (історія, `previous_value`); `repository.current_records`/`recent_records`. Розрізнення backfill/свіже — дата (`announce_worthy`, `FRESH_DAYS`), тому імпорт історії тихий. Хук `_records_check_for_user` у morning-тіку (після автоаналізу, 🎉-повідомлення), `/records`-команда, `records` у контексті `run_analysis`/`run_digest` (**і в dedup-cache key**), CLI `backfill-records`; `tests/test_records.py` |
| [EP-07](EP-07-weekly-digest.md) | Тижневий дайджест і прогрес до цілі | `SYSTEM_DIGEST` + `run_digest`/`digest_with_stats`/`_digest_cache_key` (`app/analysis/service.py`, числа рахуємо ми у `_week_volume_summary`), `weekly_digest_job`/`_digest_for_user`/`force_digest_for_user` (`bot/jobs.py`, недільна `run_daily` о `DIGEST_HOUR`, once-a-week guard `bot_state` `digest:<iso-week>`), прихована `/test_digest`, `ReportLog(kind="digest")`; `tests/test_digest.py` |
| [CODE-04](CODE-04-jobs-boilerplate-helpers.md) | Спільні хелпери per-user джоб | `eligible_users` (`app/db/users.py`) + `for_each_user`/`user_garmin_runtime` (`bot/jobs.py`): три джоби (`morning_job`/`plan_sync_job`/`plan_adapt_job`) стали 1–5-рядковими, per-user try/except централізований (одна помилка не рве цикл — тепер і в adapt), guard «є Garmin-креди» спільний для `_tick_for_user`/`_sync_for_user`; логи скіпів/падінь незмінні; `tests/test_jobs.py` |
| [CODE-05](CODE-05-shared-report-delivery.md) | Спільний report-флоу (бот/веб/morning) | `app/analysis/delivery.py::build_report` (payload → run_analysis → text + sync-прапори) + `ReportResult`/`STALE_NOTE`; три виклики зведені: `bot/handlers.py::report`, `app/routers/reports.py::report_json`, `bot/jobs.py::_deliver_morning`. Канал сам формує stale-примітку (Telegram-префікс vs JSON-`note`) й ловить `AnalystError`; morning лишає свій `_MORNING_STALE` («звіт» не «аналіз» — свідома відмінність, бо stale рахує строгіший `_recovery_synced`, не `synced_today`) + guard/вікно/MFA. Дедуп-кеш незмінний (хелпер лише збирає наявний виклик). `tests/test_delivery.py` + `test_routers.py::test_report_json_uses_shared_helper_and_stale_note` |
| [CODE-06](CODE-06-dedup-plan-edit-adapt-stats.md) | Злити `plan_edit_with_stats`/`plan_adapt_with_stats` (AST-ідентичні) | Спільний рушій `_plan_ops_with_stats(context, api_key, *, system, kind, log_label, error_msg)` (`app/analysis/service.py`); обидві публічні функції — тонкі обгортки (сигнатури незмінні — тести їх monkeypatch'ать). Нуль поведінкових змін: `kind`/лог-формат/ретрай/свідомо-не-кешований шлях збережені. CODE-01 ще не зроблено → хелпер поки в `service.py`. PR #119 |
| [PERF-04b](PERF-04b-async-anthropic-threadpool.md) | Claude-виклики з-під спільного threadpool + груповий фетч днів | Виділений `ThreadPoolExecutor` (`CLAUDE_MAX_WORKERS`=4, `thread_name_prefix="claude"`) + хелпер `_run_claude` (`app/analysis/service.py`) — усі `*_with_stats`-виклики пішли з anyio-пулу, щоб LLM-латентність не морозила Garmin-фетчі (обрано executor, а не AsyncAnthropic — сигнатури `*_with_stats` незмінні, тести їх monkeypatch'ать, нуль змін у retry/`AnalystError`/`ReportLog`). `build_payload_cached` тягне всі пропущені дні (+сьогодні) одним `run_in_threadpool`-заходом через `_fetch_days` замість hop-на-день; `tests/test_perf.py` |
| [ST-05](ST-05-strength-preview-in-form.md) | Прев'ю згенерованої силової в setup-формі | Кнопка «Прев'ю» біля кожного free-text опису → `POST /plan/strength/preview` (`plan.py::strength_preview` → `service.run_strength_preview`, той самий контекст/модель, що генерація; `ReportLog(kind="strength")`) повертає фрагмент `_strength_preview.html` (той самий акордеон) з санітизованою сесією + `_desc_hash(опис)` у `data-*`. На сабміті — hidden inputs `strength_preview_<slug>`/`strength_prehash_<slug>`; `_confirmed_previews` бере лише сесії зі збіжним hash (змінив опис → інвалідація), **пере-санітизує** кожну на бекенді (не довіряти клієнту) → `intake["strength"]["custom_generated"]`; `run_plan_generation` реюзає підтверджену сесію (без повторного Claude-виклику). Прогресивне покращення (без JS форма працює як раніше); `tests/test_strength_preview.py` + `test_routers.py` |
| [SEC-01](SEC-01-web-login-hardening.md) | Веб-логін: rate limit, секрет сесії, logout POST | In-memory sliding-window `RateLimiter` (`app/core/ratelimit.py`, per-process — свідомо) на `POST /login` (per-IP+per-email) і `POST /register` (per-IP), `LOGIN_RATE_LIMIT`/`LOGIN_RATE_WINDOW_S` (0 вимикає, тести їх обнуляють); порожній `APP_SECRET_KEY` → гучний `logging.error("AUTH: APP_SECRET_KEY is not set…")` + ефемерний per-process Fernet-ключ замість константи (`app/main.py`, сесії не переживуть рестарт, але їх не підробиш) + банер на `/login`; `/logout` → POST (форма-кнопка в `_nav.html`), старий GET лишений безстановим редіректом на `/settings`; `tests/test_ratelimit.py` + `test_routers.py` (429 після N, вікно, logout GET не розлогінює) |
| [OPS-02](OPS-02-sqlite-backups.md) | Бекапи `garmin.db` | `scripts/backup_db.py` — онлайн-safe `VACUUM INTO` (fallback backup API) → `backups/garmin-YYYY-MM-DD.db` + ротація (7 daily / 4 weekly), шлях з `settings.DATABASE_URL` (не хардкод), `--rsync-dest` для off-SD копії; `deploy/systemd/garmin-backup.{service,timer}` (щоніч 03:15), `scripts/migrate.sh` (бекап → `alembic upgrade`); restore-тест реально прогнаний (`tests/test_backup_db.py` відкриває копію й читає рядки); нотатки про окремий бекап `APP_SECRET_KEY` — у CLAUDE.md. Розгорнуто на Pi (2026-07): таймер увімкнено, off-SD rsync на USB (`/mnt/backup/garmin/`), restore-тест на реальних даних пройдено |
| [PERF-03](PERF-03-postgres-and-indexes.md) (індекси) | Аудит індексів під user-scoped читання (Postgres-частина ще ❄️ frozen) | Композитні індекси `activities(user_id, date)`, `report_logs(user_id, created_at)`, `planned_workouts(plan_id, date)` (`app/db/models.py` + міграція `f9a0b1c2d3e4`); `daily_metrics(user_id, date)` уже покритий `uq_daily_user_date`. Гарячі читання фільтрують по scope-колонці + сортують по даті/часу |
| [NF-05](NF-05-multisport-weekly-budget.md) | Мультиспорт-бюджет тижня (кайт/теніс/вело як навантаження) | `app/multisport.py` — чистий (нуль LLM) крос-спорт TRIMP-бюджет: `weekly_load` рахує навантаження по ВСІХ типах активностей в одну шкалу (HR-Edwards `dur×1–5`, fallback на тривалість за видом спорту — кайт/теніс HR шумний), розклад по видах + `non_run_pct`. `repository.weekly_activity_load` (усі типи) → `service._build_multisport` (`{weeks, this_week}` через `budget_summary`). Прокидається в контекст **генерації плану**, **адаптації** (EP-02) і **дайджесту** (EP-07) — і в `_digest_cache_key` (наскрізна пастка); промпти уникають важкого бігу впритул до об'ємного іншого спорту + стримують обсяг при високому `non_run_pct`. Свідомо НЕ в щоденному звіті. Сезонний intake — документований future extension. `tests/test_multisport.py` |
| [NF-06](NF-06-compare-past-self.md) | «Я-минулорічний» — порівняння з минулим собою | `app/compare.py` — чистий (нуль LLM) добір зіставних вікон: `window_pair` (те саме календарне вікно N років тому, Feb-29-safe), `parse_period`, `has_signal`, `fmt_range`. `repository.window_stats` агрегує вікно (обсяг/медіанний темп/HR/HRV/сон/RHR/VO2max/race-прогнози). `service.run_compare` збирає обидва вікна → один Sonnet-виклик (`SYSTEM_COMPARE`, наголос на чесність сезонів/умов) → `ReportLog(kind="compare")`, dedup-кеш (`_compare_cache_key`), `None` без історії в обох вікнах. Команда `/compare [тижнів]` (чистий DB-рід, без Garmin/MFA) + місячний автоблок у дайджесті (`_monthly_compare_for_user`, guard `compare:<YYYY-MM>`). `tests/test_compare.py` |
| [PERF-05](PERF-05-per-user-fetch-lock-and-garmin-rate-limit.md) | Rate limit/backoff до Garmin + per-user fetch-lock | Процесний `_RateLimiter` (leaky-bucket spacer, `GARMIN_RPS`=3, синхронний `threading.Lock`+sleep) у `client._api` — усі connectapi йдуть через нього; 429-розпізнавання (`_is_rate_limited`, nested `.error.response`) + експо-backoff `GARMIN_RETRIES`=2 ретраї, далі виняток пропагує (поточна поведінка: `_safe` лог+skip). MFA-гейт не чіпається (окремий шлях). Per-user `asyncio.Lock` (`WeakValueDictionary`) навколо фетч-фази `build_payload_cached` + 30-с memo `_recent_payload`: другий конкурентний виклик того самого юзера чекає й переюзає свіжий payload (`new_activities=[]` — без подвійного автоаналізу); `tests/test_perf.py` |
| [NF-04](NF-04-injury-risk-radar.md) | Травматичний радар (injury-risk сигнали) | `app/injury.py` — чистий (нуль LLM) детектор: зливає 4 ранні сигнали (повторюваний біль ≥2× за 14 дн з EP-12 — найважчий; стійкий високий ACWR ≥140% ≥3 дн; RPE росте за того ж темпу; HRV нижче базлайну + дрейф RHR) у severity-скор → `Assessment` (calibrating/none/elevated/high). Калібраційний гейт (`INJURY_MIN_HISTORY_DAYS`=14 — правило проти false positives як в EP-08). Рідери `read_load_history`/`recent_subjective_runs`/`count_daily_metrics` → `service.build_injury_assessment` (нуль LLM, спільний для `/risk` і джоби). `service.run_injury_check` наративізує actionable через Sonnet (`SYSTEM_INJURY`, консервативно, не медично) з **детермінованим fallback `injury.summary`** (попередження не залежить від LLM), `ReportLog(kind="injury")`. Команда `/risk` + хук у morning-тіку (`_injury_check_for_user`, гард `injury_warned` раз на `INJURY_GUARD_DAYS`=5). `INJURY_RADAR` on/off. Глибша інтеграція з EP-02 (авто-deload) — future step. `tests/test_injury.py` |

## Наскрізна пастка

Все, що додається в контекст Claude-виклику, **мусить увійти в dedup-cache key**
(`app/analysis/service.py::_cache_key`), інакше кеш віддаватиме старий звіт без
нового контексту. Стосується всього нового контексту (EP-07-дайджест, NF-01-норми
тощо); ST-03 — ні: `weather` уже в ключі.

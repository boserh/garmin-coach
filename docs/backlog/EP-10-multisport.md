# EP-10 · Мультиспорт: вело/плавання/трейл у планах і аналізі

**Тип:** епік · **Оцінка:** XL (фазами; фаза 2 винесена) · **Пріоритет:** фази
3/4 — за запитом (не-біговий сезон / triathlon-ціль); фаза 1 (аналіз) ✅ **зроблено**
(2026-07); фаза 2 (навантаження) виділена в [NF-05](NF-05-multisport-weekly-budget.md),
теж зроблена · **Залежності:** немає

**Статус фази 1 (2026-07)**: `app/garmin/client.py::fetch_activity_series` тепер бере
`sport=` і читає інший набір Garmin `/details`-дескрипторів для вело (швидкість км/год +
потужність Вт замість темпу); `app.garmin.service._activity_rows` реюзає NF-05's
`multisport.sport_bucket`, щоб рішення «тягнути series чи ні» не дублювало другий список
ключових слів; `reports._segments`/`activity_payload` узагальнені на будь-який набір
метрик у точці серії; `SYSTEM_ACTIVITY` і web-детейл-чарти (`app.charts.run_charts`)
говорять мовою вело, коли дані вело. Плавання (SWOLF/басейн) свідомо не займали — див.
підводні камені нижче, лишається за фазою 1b/за запитом. `tests/test_activity_series.py`.
Детально — CLAUDE.md, «Multisport activity analysis (EP-10 phase 1)».

*(переписано за аудитом 2026-07 — ANALYSIS.md §1.2: власник **уже** їздить на
вело, кайтить і грає в теніс — навантаження цих спортів сьогодні невидиме для
ACWR-контексту плану, тому «бюджет навантаження» відв'язано від вело-series і
воно не чекає решти епіка)*

## Сторя

> Як користувач, що не тільки бігає, я хочу, щоб план умів вело/плавання
> (кросс-тренінг як повноцінна сесія, не «пропуск»), аналіз активності розумів
> потужність/каденс на вело і темп на 100 м у басейні, а тижневе навантаження
> рахувалось по всіх спортах — щоб система була тренером, а не тільки біговим.

## Контекст

Зараз усе run-центричне за побудовою: `series` тягнеться тільки для бігу
(pace min/km), `weekly_run_volume` рахує лише біг, `SYSTEM_PLAN` прописує бігові
сесії + силові, `workout_export.build_workout` мапить тільки running-степи
(pace targets), matching EP-01 матчить біг до бігу. Але `activities` вже
**зберігає всі типи** (обмежень на type немає), і Garmin-календар уміє
вело/плавання-workouts тим самим API.

## Скоуп (різати на фази!)

- **Фаза 1 — аналіз** (за запитом): `series` для вело (швидкість/потужність/HR
  замість темпу), спорт-специфічні `_segments` і `SYSTEM_ACTIVITY`-гілки;
  `/activity` для вело і плавання говорить мовою відповідного спорту.
- **Фаза 2 — навантаження**: **винесена в NF-05** як самостійна фіча з власним
  пріоритетом. Вона не залежить від фази 1: кайт/теніс/вело в `activities` вже
  зберігаються, потрібна лише агрегація (TRIMP-подібне з HR).
- **Фаза 3 — план** (за запитом): intake форми питає про доступні спорти;
  `SYSTEM_PLAN` вміє класти вело/плавання-сесії (`PlannedWorkout.type`), пуш у
  календар — `sportType` 2 (cycling) / 4 (swimming) у `workout_export`.
- **Фаза 4 — triathlon-ціль**: окремий intake-режим (це вже свій епік де-факто).

## Acceptance criteria (фаза 1 як перший зріз)

- [x] Вело-активність має `series` і осмислений `/activity`-розбір.
- [x] Бігова частина не деградує (усі наявні тести зелені).

(Тижневе навантаження по всіх спортах у плановому контексті — AC тікета NF-05.)

## Підводні камені

- Плавальні метрики в Garmin специфічні (SWOLF, басейн vs відкрита вода) —
  не тягнути все, почати з дистанції/часу/темпу на 100м.
- Найбільший ризик — роздути `SYSTEM_PLAN`; можливо, кожен спорт — окрема
  секція prompt'а, що включається за intake.
